pipeline {
    agent { label 'slave' }

    stages {
        stage("Welcome Note") {
            steps {
                script {
                    println "Welcome to DevOps"
                }
            }
        }
    }
}
